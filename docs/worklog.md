# worklog（跨 harness 接力日志，append-only）

> 格式：`日期 | harness | 做了什么 | 下一步/坑`。每次会话结束追加一行，只追加不整理。

2026-07-09 | 人工+Claude | 定稿 PLAN.md（docs/PLAN.md），建 AGENTS.md/CLAUDE.md | 从 M0 开始实施
2026-07-12 | Claude Code | M0+M1 完成：脚手架/lib/Makefile/docs 骨架/15 单测；epoch+sdk+news 三源真实落盘（含 samples 三件套）；build 骨架页 90KB 离线可开 | M2 高风险源探测（openrouter/vercel/gpu，各写 ADR）；M3 ARR 锚点收集可并行。坑：Python 本机是 3.14（无 3.12），venv 正常；epoch CSV 实测 72 站点 11.2GW
2026-07-12 | Claude Code | M2+M3 完成：openrouter（frontend rankings ×3 端点，ADR-002）、vercel_gateway（RSC flight rawData 61 天，ADR-003）、gpu_prices（Vast.ai API，OCPI=Ornn 不可得，ADR-004）三源落盘+三件套；ARR 锚点 34 条（bootstrap 自 Epoch revenue CSV）+ derive/arr.py 加权对数拟合 + 8 单测；build 内嵌 7 数据块 319KB | 下一步 M4：panels/*.j2 + renderPanel + Chart.js vendor + 实时计数器。坑：vercel 是份额%非绝对量；openrouter 无日频端点（滚动 7 天口径）；vast gpu_name 拼错静默返回空
2026-07-12 | Claude Code | M4 完成：Chart.js 4.5.1 vendored；build.py 重写为 spec 函数 + 17 个 panels/*.j2 + renderPanel 薄封装 + ARR 每秒跳动计数器 + 新鲜度徽章 + Methodology 区；neo-brutalist 主题对齐参考图；gzip 97KB，file:// 双击可开，26 单测全绿。M5 的 daily.yml 已写好（cron 00:30 UTC + 失败自动开 Issue + Pages 部署）| 剩余：建 GitHub repo、开 Pages（Settings→Pages→Source: GitHub Actions）、跑通 3 天。坑：内嵌浏览器面板滚动截图有合成故障，验证页面用 headless Chrome --screenshot 更可靠；epoch timelines 与 sites 两表口径独立（14.3 vs 11.2 GW），增长图注释已写明
2026-07-12 | Claude Code | M5 完成：gh 建仓 CharlesLiuyx/Token-Economy-Tracker（先私有推送，用户确认后转公开）、Pages 开启（build_type=workflow）、手动触发 daily-update 全绿（六源在 runner 上全部抓通、data: 2026-07-12 提交、Pages 部署 200）。线上 https://charlesliuyx.github.io/Token-Economy-Tracker/ | 验收观察：连续 3 天（至 07-15）cron 自动成功；小噪音：actions/checkout@v4 等有 Node20 弃用警告，之后升 @v5/@v6
2026-07-12 | Claude Code | 建每日巡检 Routine（token-tracker-daily-patrol，本地 09:10，Opus 4.8）：检查 daily-update 运行状态 + data/_status.json 单源缺数 + pipeline-failure Issue，故障时逐级升级修复（重跑回补→适配上游→换替代通路→修 workflow），仅客观不可修才留人工；纯巡检不产生 commit | 观察 Routine 与 Actions 配合是否顺畅；07-15 M5 验收到期后可精简 prompt 中的 M5 检查项
2026-07-13 | Claude Code | 手工加 2 条 ARR 锚点（YipitData 口径，截止 2026-06-30：Anthropic 70B、OpenAI 43B，均 run_rate_arr/likely）；derive.arr 重算 MoM 27.7%/11.8% 在合理区间、26 单测全绿、make build 重生成 index.html(285KB) | 坑：source_url 用 yipitdata.com 首页占位，用户如有具体报告链接可替换；fetch-arr 不是真 target（ARR 是 derive 步，用 python -m scripts.derive.arr）
2026-07-13 | Claude Code | UI 重构为 Linear 风格：style.css 全量重写（中性色 token 双主题，:root light 缺省 + [data-theme=dark] 覆盖）、顶栏锚点导航 + 主题切换按钮（localStorage 覆盖系统偏好，head 内联脚本防白闪）、图表随主题销毁重建（app.js 读 CSS 变量注入 Chart.defaults，spec "__surface" 占位符替换）、build.py PALETTE 换双主题可读中饱和色（0=Anthropic clay/1=OpenAI teal）并去黑描边、模板去 tone/dots/fake-x 装饰；动效原则最终定为瞬时到位：顶栏锚点跳转去掉 scroll-behavior:smooth（用户嫌粘滞的真凶，原生平滑滚动不可调速）、主题切换硬切（先后试了 120ms ease-out、expo、View Transitions 均弃，仅留按钮按压回弹）；坑：图表同步重建与 CSS 过渡同帧启动会吃掉动画首帧（图表瞬建，JS 实测 ~30ms）；gzip 99KB、26 测全绿、双主题浏览器实拍验证；同步 frontend.md/PLAN.md | 下一步：M5 观察至 07-15。坑：内嵌浏览器面板 scrollY>0 截图全白（已知合成故障），全页验证改用拉高视口一次截完
2026-07-13 | Claude Code | 远端信息英文化：GitHub About 描述换英文（gh repo edit）、新增英文 README.md（live 链接/架构/数据源表/快速开始/硬约束/commit 规范）、conventions.md 增 Commit message 规范节（口径指向 README，英文祈使句 <type>: <summary>） | 后续 commit message 按新规范写英文；本行随下批提交入库（工作区尚有未提交 UI/i18n 改动）
2026-07-13 | Claude Code | 顶栏加 GitHub 跳转 icon：index.html.j2 topbar 在 BUILD 时间戳与语言切换钮之间加 <a class="icon-btn">（GitHub logo SVG，target=_blank rel=noopener，链到 https://github.com/CharlesLiuyx/Token-Economy-Tracker），复用现有 .icon-btn 样式；i18n.yml 加 ui.github（aria-label/title，data-i18n-* 走运行期切换）；make build 重生成 index.html、29 测全绿、浏览器实拍验证 | 下一步：M5 观察至 07-15
2026-07-13 | Claude Code | i18n 架构落地（zh/en 运行时切换，ADR-005）：文案唯一出处 site/template/i18n.yml（key->{zh,en}，加载即校验）；模板 t(key) 渲染默认 zh + data-i18n/-html/-title/-aria-label（参数用 data-i18n-args {name} 占位）；图表 spec 经 build.py L(key) 生成 "__i18n:<key>" 占位符（可嵌 f-string 长串），app.js resolveSpec 与 __surface 同管线正则解析，语言切换复用 renderAll 销毁重建；偏好 localStorage lang > navigator.language，head 首帧前设 data-lang，<html lang> 随切换；顶栏加 lang-btn（显示切换目标 EN/中）；新增 tests/test_i18n.py 三道闸（双语齐全/{args} 一致/引用与目录双向对齐防死 key，坑：文件名 chart.umd.min.js 误匹配 chart.* 命名空间→py 侧只扫 t()/L() 调用）；29 测全绿、make build 308KB、浏览器实拍双向切换+主题共存验证；同步 frontend.md i18n 节 | 下一步：M5 观察至 07-15；新语言=LANGS 加项+i18n.yml 补列
2026-07-13 | Claude Code | push 即部署：新增 .github/workflows/deploy.yml（push 到 main 命中 site/scripts/data/requirements 路径时 make build → upload-pages-artifact → deploy-pages，只构建不提交不抓数，避免与 daily.yml bot push 循环）；daily.yml 的 deploy-pages job 加 concurrency:pages-deploy 与之互斥；同步 PLAN §8 加"push 即部署"节 | 下一步：改动推上去后验证 deploy-on-push run 成功、线上刷新
2026-07-18 | Claude Code | UI 品牌改名：顶栏 brand 与 <title> 由「AI Monetization Tracker」改为「Token Economy Tracker」（与仓库/线上一致）；去掉 page-head 头部 hero（<h1> + tagline 副标题），保留下方 source-line 数据新鲜度徽章；随之删除死键 ui.tagline（i18n.yml）与死样式 .page-head h1 / .tagline（style.css，避免 test_i18n 死键失败与残留 CSS）；make build 重生成 index.html(308KB)、29 测全绿、grep 确认 0 处 Monetization 残留 | 下一步：改动推上去后线上刷新确认（deploy-on-push）
2026-07-24 | Claude Code | 加 2 条 ARR 锚点（Yipit 口径 2026-07-12：Anthropic 79B、OpenAI 50B，run_rate_arr/likely，源同前用 yipitdata.com 占位=私有订阅无公开链接）；derive/arr.py 拟合改造为「形态由数据选」：候选 linear（y=b·(t−t₀)，log 空间闭式+t₀ 一维搜索）/ exponential / log_quadratic 各拟合一次，用滚动起点回测（前 i 个锚点预测第 i+1 个，折按目标点近期权重加权）选 RMSE 最小者、同分取参数少者，MoM 越界候选出局、锚点排不出折时退化为取最简形态；外推改为冻结增速（用最后锚点处瞬时对数增速直线延伸，曲率不外推）；置信带改为预测区间 σ²+gᵀcov(β)g（外推越远越宽，Anthropic ×1.54→×2.23@+190d），并新增 _trim_unsupported 裁掉早期带宽>3× 的无支撑段（只留散点）。实测选中：Anthropic=log_quadratic（回测 0.2346 vs 指数 0.2776，拟合值贴住最后锚点 77.6B/79B，旧全局指数落后 12%）、OpenAI=exponential（0.1100 vs logquad 0.1199，即 logquad 在 OpenAI 上是过拟合）。fit.candidates 落盘全部候选的回测/σ/MoM/落选原因；卡片加「形态」chip（3 个 i18n key + model_title 说明）；同步 arr-methodology.md 重写与 PLAN §6；39 单测全绿（新增 10 个：形态选择 3 种合成数据各选对、外推 log 空间等步长、带宽单调张开、无支撑段裁剪、稳定指数不裁）、make build 308KB、浏览器实拍 zh/en 双语验证 | 下一步：新披露照常追加锚点即可，形态会自动重选；若 Anthropic 增速回落，回测会自动把 log_quadratic 换回 exponential。坑：i18n.yml 里含「: 」的英文文案必须加引号（YAML ScannerError）；模板里拼接的 i18n key 扫不到会被 test_i18n 判死键，故 model_keys 映射写成字面量
2026-07-24 | Claude Code | 清掉 Node 20 弃用告警：两个 workflow 的 action 全升当前大版本 —— checkout v4→v7、setup-python v5→v7、upload-pages-artifact v3→v5、deploy-pages v4→v5、github-script v7→v9（M5 那条留的尾巴）。逐条核对过破坏性变更对不上我们的用法：checkout v6 只把凭据挪到 $RUNNER_TEMP 独立文件、官方明示 git push 照常（daily 的 bot push 依赖它），v7 只挡 pull_request_target/workflow_run 的 fork PR（我们不用这两个事件）；setup-python v7 删的 pip-install 输入我们没用；upload-pages-artifact v4 起不打包 dotfiles，site/ 下无隐藏文件（git ls-files 核过）；deploy-pages v5 权限要求仍是 pages:write+id-token:write，两个 workflow 都已有；github-script v9 破坏的是 require('@actions/github') 与 getOctokit 重声明，告警脚本只用 github.rest.issues.* 和 context.* | 验证：push 触发 deploy-on-push 全绿且 annotations 清零，覆盖 checkout/setup-python/upload-pages-artifact/deploy-pages 四个；github-script@v9 那步是 if:failure() 才跑，没为验证它去人为搞挂 daily，按静态核对结论放行，下次真失败时会实跑
2026-07-24 | Claude Code | 全站图表加 legend hover 高亮：app.js 在 renderPanel 里给每图挂 legend.onHover/onLeave -> setEmphasis(chart, datasetIndex)，把非当前组系列的十六进制色淡出到 15%（fade() 处理 #RGB/#RGBA/#RRGGBB/#RRGGBBAA 与数组色，原色首次 hover 时缓存在 chart.$emphasis，onLeave 全量还原），同时给 canvas 上 pointer 光标；分组用新占位 dataset.__group（不写则每系列自成一组），build.py spec_arr 给同公司 6 条 dataset 都打上 __group=company，于是 hover「anthropic fit」时该公司的置信带/外推虚线/锚点/披露值一起亮、OpenAI 整组淡出。主题/语言切换走原有销毁重建，$emphasis 随实例丢弃无残留 | 验证：make build 311KB、39 测全绿、浏览器实拍 ARR 图与 or_tracked 图 hover 前后对比 + 读 dataset 色确认 onLeave 精确还原 | 下一步：无。坑：预览 pane 隐藏时 rAF 挂起，Chart.js 的事件回调是 rAF 节流的，纯 dispatchEvent 验证会「没反应」，要用真 hover + 截图逼出一帧
2026-07-25 | routine | 每日巡检：daily.yml 最近数次全绿；发现 sdk_downloads 07-24 单源失败——pypistats.org 429 限流（tolerated，workflow 仍绿），07-24 快照缺失（daily 目录 07-23 直接跳 07-25）。curl 复测 pypistats 已回 200，判定偶发抖动。refetch 窗口内回补 07-24 快照（npm+pypi 全量），make test 39 绿、make build 311KB。回补时今日 07-25 cron 已跑完并 push 了 data:2026-07-25，故 reset 到 origin/main 后只补 07-24 历史快照、不动 07-25 的 latest/_status，重建站点带上这个历史点。其余源均 07-25，无缺口；无 pipeline-failure Issue | 下一步：无；若 pypistats 反复 429 再考虑加退避重试
2026-08-02 | routine | 每日巡检：daily.yml 最近 5 次全绿；发现 sdk_downloads 08-01 单源失败——pypistats.org 429 限流（tolerated，workflow 仍绿），08-01 快照缺失（daily 目录 07-31 跳 08-02 缺 08-01）。这是 07-24 之后同一 429 的第二次复发。make fetch-sdk_downloads 复测已回 200（限流窗口已过），判定偶发抖动。refetch 窗口内回补 08-01 快照（npm last-day start/end=2026-08-01 正好对齐目标日，全量 npm+pypi），并落 08-02 今日快照；make test 39 绿、make build 312KB 重建站点带上这两点。net.py 已有 3 次指数退避（2s+4s，429 亦重试），本次限流窗口超过 ~6s 退避故 CI 内未自愈——但属设计内容忍（单源失败不阻断、latest 留旧值+stale 徽章、回补恢复），未在无人值守巡检中改动全源共享的 net.py | 下一步：无需人工。若 pypistats 429 转为高频，durable fix 是让 net.get 在 429 时读 Retry-After 头做定向长退避（而非当前面向 5xx 的短退避），需配 net 单测后再上；当前 2 次/周属低频，暂不动共享层
2026-08-06 | routine | 每日巡检：daily.yml 最近 5 次全绿。发现 sdk_downloads 08-05 单源失败——pypistats.org 429 限流（tolerated，workflow 仍绿），08-05 快照缺失（daily 目录 08-04 跳空）。这是 07-24 / 08-01 之后同一 429 的第三次复发（≈每周一次）。巡检期间 08-06 cron 恰好跑完并 push（其 sdk_downloads 已回 200、latest 前进到 08-06），但 08-05 空洞仍在。curl/复跑复测 pypistats 200，判定偶发抖动。在 refetch 窗口内回补 08-05 历史快照（--date 2026-08-05，npm last-day start/end=2026-08-05 正对齐目标日，npm+pypi 全量）；因 latest 已是权威的 08-06，reset 到 origin/main 后只补 08-05 daily 一个历史点、不回滚 latest/_status，make test 39 绿、make build 重建站点带上该点。其余源均 08-06，无缺口；无 pipeline-failure Issue。未改共享 net.py：429 已被设计内容忍（单源失败不阻断+backfill 自愈），pypistats 限流窗口常达分钟级，读 Retry-After 做阻塞式长退避会拖挂 CI，风险收益不划算 | 下一步：无需人工。若 429 转高频（>1/周）优先换通路而非阻塞重试：错峰抓取或改 pypistats bulk/BigQuery 口径；net 层改动须配单测、不在无人值守巡检中上。
2026-08-10 | routine | 每日巡检：daily.yml 最近 6 次全绿（含今日 08-10 02:40Z 那次，已自愈）。发现 sdk_downloads 缺 08-09 一天——08-09 那次 cron 撞 pypistats 429（tolerated，workflow 仍绿），daily 目录 08-08 跳 08-10。这是 07-24/08-01/08-05 之后同一 429 的第四次复发。这次做了 durable fix（前几次 worklog 一直 defer 的那个，前提是「配 net 单测」）：诊断确认 pypistats 是**按 IP 突发限流**（curl 实测 ~8s 恢复、无 Retry-After 头，并非之前推测的分钟级），根因是 3 个 pypi 包无间隔连拉撞突发桶。改动：(1) net.get 把 429 从「面向 5xx 的 2s/4s 短退避」提为定向长退避（8s→16s→24s，尊重更长的 Retry-After），retries 3→4；(2) sdk_downloads.py 对 pypi 请求间留 6s 间隔（PYPI_REQUEST_SPACING_S）。新增 tests/test_net.py 4 例覆盖 429 重试/耗尽/Retry-After/正常无 sleep。refetch 窗口内回补 08-09（--date，npm last-day start/end=2026-08-09 正对齐目标日，npm+pypi 全量，回补时 429 已被新重试逻辑自愈）。make test 43 绿、make build 314KB 重建站点带上该点。同步更新 docs/sources/sdk_downloads.md「坑」段+核对日期。其余源均 08-10，无缺口；无 pipeline-failure Issue | 下一步：观察后续 cron 是否还再撞 429（间隔+退避应显著降频）；若加更多 pypi 包注意单源总耗时随包数线性增长
2026-08-14 | routine | 每日巡检：daily.yml 最近数次全绿（含今日 08-14 03:05Z 那次，sdk_downloads 已自愈回 200、latest 前进到 08-14）。发现 sdk_downloads 缺 08-13 一天——08-13 那次 cron 撞 pypistats 429（google-genai，tolerated，workflow 仍绿），daily 目录 08-12 跳 08-14。这是 07-24/08-01/08-05/08-09 之后同一 429 的**第五次复发**，也是 08-10 durable fix（6s 间隔 + 8s→16s→24s 定向退避 + retries=4）落地后的**首次复发**（间隔从 fix 前的约每周一次拉长到约 3-4 天）。判定仍属偶发抖动而非 fix 失效：本次手动 --date 2026-08-13 回补，首跑即 200 成功；只有我紧接着的第二次快速复跑才再撞 429（是我个人把 per-IP 突发桶耗尽），等 90s 后重试即恢复——印证 fix 有效、限流是按 IP 突发桶。08-13 那次 CI 内退避（最长约 48s）仍耗尽，最可能是 GitHub Actions runner 共享出口 IP、邻居 job 先耗了突发桶，我方 job 内加间隔无法规避共享 IP 的节流。refetch 窗口内只回补 08-13 一个历史 daily 点（--date，npm last-day start/end 对齐 08-13，npm+pypi 全量）；因 latest 已是权威的 08-14，reset 到 origin/main 后不回滚 latest/_status（store 对 08-13<08-14 自动不更新 latest）。注意：spec_sdk 只读 latest.json 出「当前周下载量」柱状图、不消费 daily 序列，故本回补只填数据档案空洞、不改站点渲染；data/** 命中 deploy.yml 触发器会自动 make build 重部署。make test 43 绿。其余源均 08-14，无缺口；无 pipeline-failure Issue | 下一步：无需人工。继续观察复发间隔——若回落到 fix 前的每周甚至更频，再上更强手段（当前 defer 中）：给退避加 jitter+抬高上限、或错峰/改 pypistats bulk 与 BigQuery 口径绕开按 IP 突发限流；net/共享层改动须配单测、不在无人值守巡检中上。单次 post-fix 复发不足以支撑改共享层。
2026-08-16 | routine | 每日巡检：daily.yml 最近 5 次全绿（最新 08-15 01:56Z，latest 曾停在 08-14）。发现 sdk_downloads 缺 08-15 一天——08-15 cron 撞 pypistats 429（google-genai，tolerated，workflow 仍绿），daily 目录 08-14 跳空。这是同一 429 的**第六次复发**、也是 08-10 durable fix（6s 间隔+8s→16s→24s 退避+retries=4）后的**第二次**（08-13 后约 2 天，复发未如预期继续拉长）。复现时确认关键新事实：**pypistats 的 429 响应体仍带完整有效数据**（curl 实测 429 body 里 openai/anthropic/google-genai 的 last_day/week/month 均在，限流只改状态码、数据走 CDN 缓存照发）。据此上真正的 durable fix（package 局部、不动共享 net 层）：sdk_downloads.py 新增 _pypi_recent()，net.get 退避重试后仍 429 时，只要 body 能解析出 data 就采用它，仅 429+无 data 才判失败——直接击穿「间隔+退避都躲不掉」的持续限流（尤其 CI 共享出口 IP 被邻居 job 耗尽突发桶，正是 08-13/08-15 复发主因）。加 import requests；含 6s 间隔时最坏仍先走完退避再取 body（可接受）。回补 08-15（--date，npm+pypi 全量；因 08-15>08-14，store 令 latest 前进到 08-15、_status 转绿）。手写 2 例烟测证 429-with-body 采用、429-no-body 抛出；make test 43 绿、make build 314KB 重建站点。同步更新 docs/sources/sdk_downloads.md「坑」段+核对日期 08-16；sample 结构未变不改。巡检期间今日 08-16 cron 已跑完（02:05Z 成功）并 push 了 data:2026-08-16，故 reset/ff 到 origin/main（latest 已前进到 08-16）后只补 08-15 一个历史 daily 点、不回滚 latest/_status。**另发现 news 缺 08-16**——08-16 cron 撞 Google News RSS 503（tolerated，freshness 仍 ok 因容 1 天），news latest 停在 08-15；make fetch-news --date 2026-08-16 首跑即 200（503 属上游偶发抖动），回补 08-16 使 news latest 前进到 08-16、_status 转绿。至此七源 _status 全绿、快照日期均 08-16，无缺口；无 pipeline-failure Issue | 下一步：观察后续 cron——sdk 此 fix 应让 429 不再导致丢天（有 body 即落数）；若 pypistats 改为 429 空 body 或彻底封禁，再评估换通路（错峰/BigQuery/pepy）。news 503 属偶发，无需动作。
2026-08-18 | Claude Code | 按 Bloomberg 08-17 报道修 Anthropic ARR 曲线过高：加锚点 anthropic 2026-07-31 65B（run_rate_arr/confident，公司向投资者定期更新口径；同文 Q2 初步收入 11.5B vs 去年同期 787M、OpenAI「近期超 40B」）。新增第四种口径 `third_party_est`（VALID_METRICS + build.py 空心三角散点 + chart.arr.third_party i18n），把 4 条 YipitData 锚点（Anthropic 70B@06-30 / 79B@07-12，OpenAI 43B@06-30 / 50B@07-12）从拟合里摘出来只做展示——面板估算与公司自报不是一个口径，07-12 79B 与 07-31 65B 直接冲突；实测两者混拟合会让曲线在最后锚点处给出 81.7B（比 65B 高 26%），今日 ARR 被抬到 97B。改后 Anthropic 回测把形态从 log_quadratic 换回 exponential（0.2529 vs 0.2855），今日 ARR 115B→74.8B、MoM 38.4%→25.8%、fitted@last 65.4B 贴住 65B 锚点；OpenAI 顺带 53.2B→45.4B（最后锚点退回 25B@02-28），与 Bloomberg「OpenAI 近期超 40B」一致。同步 arr-methodology.md（口径表 + 新增「口径冲突」小节 + 回测表刷新到 08-18 + 带宽 ×1.72→×2.09）、PLAN §6、methodology 面板文案、图表标题图例；45 单测全绿（原 test_non_run_rate_metrics_excluded_from_fit 参数化到 3 种口径）、make build 316KB、浏览器实拍 zh/en 双语核对 | 下一步：Yipit 若发布与公司口径可对齐的新数据，再决定是否改回 run_rate_arr；Anthropic 若继续减速，回测会自动维持 exponential
2026-08-27 | routine | 每日巡检：daily.yml 最近数次全绿（最新 schedule 那次 08-26 02:08Z 成功）。发现今日 08-27 的 cron 未触发——巡检时（03:50Z）无 08-27 运行（非排队非进行中非失败），七源 _status/latest 仍停在 08-26。历史上 schedule 实际约在 01:56-02:08Z 触发（较 00:30Z cron 延迟约 1.5h），此刻已过常规窗口约 1.7h 仍无运行，判定为 GitHub 调度器漏跑/延迟（GitHub Actions scheduled workflow 高负载下延迟数小时或跳过是已知行为），非本仓代码/基础设施故障（近 8 次 schedule 全绿、无 pipeline-failure Issue、_status 无 error）。为保证当天数据落盘，手动 `gh workflow run daily-update`（run 33037602184，workflow_dispatch），全绿：fetch/test/build/commit + deploy-pages 均成功，push data:2026-08-27。ff 到 origin/main 后七源 _status 全绿、快照日期均 08-27、error 全 None，站点已重部署。无代码改动、无 docs 三件套变更 | 下一步：无需人工。观察明日 08-28 的 schedule 是否恢复自动触发；若连续多日 cron 漏跑（非单次延迟），再排查 GitHub scheduler 层（如仓库 60 天无活动导致 schedule 被禁用等）或加 workflow_dispatch 兜底。
2026-08-28 | routine | 每日巡检：daily.yml 最近数次全绿。发现今日 08-28 的 cron 又未在常规窗口触发——巡检时（03:50Z）无 08-28 运行，七源 _status/latest 仍停在 08-27。这是**连续第二天**漏跑（08-27 同样漏跑、经手动 dispatch 落数，且其 schedule-event 那次最终延迟到 10:06Z 才自动跑成功）。关键判据：08-27 那次 10:06Z 的运行 event=schedule 且成功，证明 **schedule 并未被禁用**（非仓库 60 天无活动导致——本仓 bot 每日 commit 保持活跃），只是被 GitHub 调度器大幅延迟数小时（高负载下 scheduled workflow 延迟/漏跑是已知行为，:30 接近整点仍属拥堵时段）。非本仓代码/基础设施故障（fetch/test/build/deploy 手动跑全绿、_status 无 error、无 pipeline-failure Issue）。为保当天落盘，手动 `gh workflow run daily.yml`（run 33140106589，workflow_dispatch）全绿：七源 fetch 全成功、45 单测绿、build、commit push data:2026-08-28、deploy-pages 成功；ff 到 origin/main 后七源 _status 全绿、快照日期均 08-28、error 全 None（昨日 sdk_downloads 的 429 也已自愈）。今日若延迟的 schedule 稍后仍自动补跑，只会对同日 08-28 数据产生 no-op/重复 commit，无害。无代码改动、无 docs 三件套变更 | 下一步：暂不改 cron 配置——仅 2 个数据点、且延迟由 GitHub 侧负载驱动而非可按分钟确定，改配置属投机性 churn 且未必有效；每日巡检本身即有效兜底（已连续两日成功补跑）。继续观察：若连续 3+ 天且 schedule-event 彻底不再自动补跑（而非仅延迟），再评估把 cron 挪离拥堵整点（如 17:mm UTC 错峰）或引入外部触发；此类改动须同 PR 更新 daily.yml 与 docs、不宜在无人值守巡检中投机上。
2026-08-29 | routine | 每日巡检：daily.yml 最近数次全绿。今日 08-29 cron 又未在常规窗口触发——巡检时（03:50Z）无 08-29 运行，七源 _status/latest 停在 08-28。这是**连续第三天**漏跑（08-27→10:06Z、08-28→11:37Z 两次 schedule-event 最终都延迟约 10-11h 才自动跑成功）。据 08-28 设定的升级判据「仅当 schedule-event 彻底不再自动补跑（而非仅延迟）才动 cron 配置」：两日证据均显示 schedule 仍自动补跑、只是被 GitHub 调度器大幅延迟（:30 接近拥堵整点、8 月下旬 GitHub 侧负载导致 scheduled workflow 延迟数小时是已知行为），故本次仍判「延迟非禁用」，维持既有兜底、不做投机性 cron 改动。为保当天落盘，手动 `gh workflow run daily.yml`（run 33232433320，workflow_dispatch）全绿：七源 fetch 全成功、单测绿、build、commit push data:2026-08-29、deploy-pages 成功；ff 到 origin/main 后七源 _status 全绿、快照日期均 08-29、error 全 None。今日若延迟的 schedule 稍后自动补跑，只对同日 08-29 产生 no-op/重复 commit，无害。无代码改动、无 docs 三件套变更；无 pipeline-failure Issue | 下一步：连续 3 日均属「延迟非禁用」，判据未越线故暂不改 cron；但 10h 级延迟已是相对历史 1.5h 的持续恶化，若再延续 2-3 日（day 5+）或某日 schedule-event 彻底不补跑，则主动把 cron 从 30 0 挪到错峰奇数分/非拥堵时段（如 23 22 UTC=06:23 京，仍赶早晨窗口），同 PR 更新 daily.yml 与 docs。每日巡检本身已连续三日有效兜底。
2026-08-30 | routine | 每日巡检：daily.yml 最近数次全绿（含 08-29 schedule-event 那次 07:15Z 成功，延迟约 6.75h、较 08-27/08-28 的 10-11h 反而收窄）。今日 08-30 cron 又未在常规窗口触发——巡检时（03:50Z）无 08-30 运行（非排队非进行中非失败），七源 _status/latest 停在 08-29。这是**连续第四天**漏跑（08-27→10:06Z、08-28→11:37Z、08-29→07:15Z 三次 schedule-event 最终都自动补跑，延迟量非单调恶化）。据 08-29 设定的升级判据「day 5+ 或 schedule-event 彻底不补跑才动 cron」：今天是 day 4、且证据仍为「延迟非禁用」（schedule 每日自动补跑、_status 无 error、无 pipeline-failure Issue），故维持既有兜底、暂不改 `30 0` cron。为保当天落盘，手动 `gh workflow run daily.yml`（run 33291214578，workflow_dispatch）全绿：七源 fetch 全成功、单测绿、build、commit push data:2026-08-30、deploy-pages 成功；ff 到 origin/main 后七源 _status 全绿、快照日期均 08-30、error 全 None。今日若延迟的 schedule 稍后自动补跑，只对同日 08-30 产生 no-op/重复 commit，无害。无代码改动、无 docs 三件套变更 | 下一步：**已到升级阈值边界——明日 08-31 即为 day 5**。若 08-31 cron 仍在常规窗口漏跑，则不再仅手动兜底，主动把 cron 从 `30 0` 挪到非拥堵时段（如 `23 22 * * *`=次日 06:23 京，仍赶早晨出数窗口）以避开整点拥堵，同 PR 更新 daily.yml 与本 worklog/PLAN；若某日 schedule-event 彻底不再自动补跑（而非仅延迟），同样立即动 cron。每日巡检已连续四日有效兜底。
2026-08-31 | routine | 每日巡检 + **达阈值执行 cron 错峰**：daily.yml 最近数次全绿。今日 08-31 cron 又未在常规窗口触发——巡检时（03:50Z）无 08-31 运行，七源 _status/latest 停在 08-30。这是**连续第五天**漏跑（08-27→10:06Z、08-28→11:37Z、08-29→07:15Z、08-30→05:38Z 四次 schedule-event 均自动补跑但延迟 6-11h）。据 08-30 worklog 设定的升级阈值「day 5 若仍在常规窗口漏跑则主动挪 cron，不再仅手动兜底」：今天正是 day 5 且确认漏跑，判据越线，故本次执行 cron 错峰改动。**动作一（保当天落盘）**：手动 `gh workflow run daily.yml`（run 33355181320，workflow_dispatch）全绿——七源 fetch 全成功、45 单测绿、build、commit push data:2026-08-31、deploy-pages 成功；ff 到 origin/main 后七源 _status 全绿、快照日期均 08-31、error 全 None。**动作二（durable：改 cron 配置）**：把 `daily.yml` schedule 从 `30 0 * * *`（00:30 UTC，撞整点/午夜拥堵）挪到 `23 22 * * *`（22:23 UTC = 北京次日 06:23，仍赶早晨出数窗口）——非整点分钟（:23）避开 top-of-hour 拥堵、非午夜时段避开 00:00 UTC 大量 daily job 拥堵，两条均是 GitHub scheduled workflow 延迟的已知主因。同步更新 AGENTS.md（00:30→22:23）与 PLAN §8（加错峰缘由）。45 单测绿、pyyaml 校验 cron 解析正确。未 make build（纯 cron 配置+docs 改动，站点已由 dispatch 的 CI 重建，避免时间戳 churn）。无 pipeline-failure Issue | 下一步：观察今晚 22:23 UTC（09-01 06:23 京）这次 schedule 是否按新槽准时/低延迟自动跑成功——若恢复正常即证错峰有效、不再需每日手动兜底；若新槽仍被大幅延迟/漏跑，则问题非「拥堵整点」而更可能是仓库侧 schedule 被限流，届时评估引入外部触发（如另一 repo 的 cron 走 workflow_dispatch API 兜底）。
2026-09-02 | routine | 每日巡检 + 回补 sdk_downloads 429 缺口：daily.yml 最近数次全绿。**cron 错峰（08-31 改 `23 22 UTC`）已见效**——09-01、09-02 两次运行均由 `schedule` 事件自动跑成功、无需手动 dispatch，延迟从此前 6-11h 收窄到约 2h（09-02 run 33574788713 于 00:18Z 成功，对应 09-01 22:23Z 槽延迟约 1.9h）；连续第五天漏跑后判据越线所做的错峰改动，验证有效，本日不再需要人工兜底触发。**唯一异常**：`data/_status.json` 显示 sdk_downloads 于 00:19Z 首个 pypi 请求（openai）即 429 失败、last_success 停在 09-01——workflow 整体成功（其余五源均落 09-02、graceful degradation 如设计），仅此单源缺 09-02。诊断：pypistats 应用级按 IP 限流，CI 共享出口 IP 在 fetch 前已被邻居 job 耗尽突发桶，且本次 429 响应体为「429 RATE LIMIT EXCEEDED」HTML 页（非带 data 的 CDN 缓存），故 `_pypi_recent` 的 429-with-body 容忍路径不适用。本地 `make fetch-sdk_downloads` 复现（openai 过、anthropic 429），直接 curl 亦 429、无 Retry-After 头。处置：按 runbook「回补数据」——静默冷却 600s（不中途探测以免重置滑动窗口）后 `.venv/bin/python -m scripts.fetch.sdk_downloads` 成功落 09-02（npm 三包 + pypi 三包数据齐全）；`_status.json` sdk_downloads 转 success（04:08:44Z、error null）。45 单测绿、make build 318KB。六源现均齐 09-02、error 全 None。无代码/docs 三件套改动（既有 429 缓解已就位，本次纯上游瞬时限流，不做投机性改码）；无 pipeline-failure Issue | 下一步：继续观察 22:23 UTC 新槽是否维持低延迟自动跑（已连两日有效，若再稳定数日可视为 cron 问题闭环）；sdk_downloads 429 属已知复发型上游限流（08-05/09/13/15 + 今日），单日缺口经巡检回补即可，若某日 CI IP 持续限流致连续多日缺数，再评估把 pypi 抓取错峰到与 npm 分开的时段或加更长退避。
2026-09-03 | routine | 每日巡检 + **durable 修复 sdk_downloads 429（连续第二日复发触发升级）** + 回补 09-03：daily.yml 最近数次全绿（09-03 run 33699043444 由 schedule 于 00:19Z 自动跑成功，对应 09-02 22:23Z 槽延迟约 1.9h，cron 错峰持续有效、无需手动兜底）。**异常同 09-02**：`_status.json` 显示 sdk_downloads 于 00:20Z 首个 pypi 请求（openai）即 429、last_success 停在 09-02；workflow 整体成功（其余五源落 09-03，graceful degradation），仅此单源缺 09-03。这是 pypistats 429 **连续第二天**打挂 daily run（09-02、09-03），越过 09-02 worklog 设定的升级线「若连续多日缺数，加更长退避」。诊断：直接 curl 三包，openai/google-genai 返 53b「429 RATE LIMIT EXCEEDED」HTML 页（**无 data**，故 08-16 的「429-with-body」容忍路径不适用），anthropic 返 200；手动分包耐心重试（12s/24s 间隔）三包均能拿到 200 → 端点约 30–60s/包即恢复，net 层 ~48s 退避在 CI 共享 IP 被邻居 job 耗尽突发桶时不够。**durable fix（行为改动，同步 docs 三件套）**：改 `scripts/fetch/sdk_downloads.py`——① 包间距 `PYPI_REQUEST_SPACING_S` 6s→10s；② `_pypi_recent` 对「429 且 body 无 data」新增包级耐心重试（`PYPI_NODATA_429_RETRIES=4` × `PYPI_NODATA_429_SLEEP_S=18s`），把「丢一天」概率压到最低；同步更新 `docs/sources/sdk_downloads.md` 坑位（两种 429 形态）。45 单测绿。**验证 + 回补**：`make fetch-sdk_downloads` 用新逻辑一次成功落 09-03（npm 三包 + pypi 三包齐全，openai/anthropic/google-genai last_week 均正数），`_status.json` sdk_downloads 转 success（03:55:29Z、error null）；make build 319KB。六源现均齐 09-03、error 全 None。无 pipeline-failure Issue | 下一步：观察下一次 22:23 UTC daily run 里 sdk_downloads 是否借新退避在 CI 内自愈（不再需巡检回补）——若连续数日 CI 内 pypi 不再缺数即证 durable fix 有效、闭环；若耐心重试仍被 CI 持续限流击穿，再评估把 pypi 抓取与 npm 拆到不同时段错峰、或改用 pypistats 的按包历史接口/BigQuery 公共数据集作替代通路。
